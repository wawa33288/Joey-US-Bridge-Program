{
  // Cloudflare Pages — Joey Year 1 U.S. History & ELA Course
  // Deploy with: wrangler pages deploy .
  // Or connect this repo to Cloudflare Pages in the dashboard for auto-deploy on every git push.

  "name": "joey-year1",

  // Static site — no build step needed, index.html is already built
  "pages_build_output_dir": ".",

  // Compatibility date required by Cloudflare Workers runtime
  "compatibility_date": "2026-01-01"
}
